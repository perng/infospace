{
  "name": "@spatial-present/cli",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Mini presentation compiler: validate / outline / narration-script export, plus the offline TTS narration renderer (generate-narration.py). Run against a journey module path with tsx.",
  "exports": {
    ".": "./src/journey-cli.ts"
  },
  "dependencies": {
    "@spatial-present/core": "*",
    "@spatial-present/schema": "*",
    "@spatial-present/worlds": "^0.1.0",
    "yaml": "^2.9.0"
  },
  "devDependencies": {
    "@types/node": "^24.12.3"
  }
}
